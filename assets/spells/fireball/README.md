# Fireball spell art (shared)

Shared animated frames for `basic_fireball` and `big_fireball`.
Scale differences live in content (`ProjectileData.radius` / `visual_size_multiplier`), not duplicate PNGs.

- `frames/` — PNG sequence
- `fireball_frames.tres` — Godot `SpriteFrames` animation `spin`
- `LICENSE.txt` — Craftpix attribution (M5 visual test exception)
