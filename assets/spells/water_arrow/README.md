# Water Arrow spell art

Craftpix Water Arrow frames used for the left-click manual attack (`basic_fireball` content id).
Art faces +X (right); projectiles set `rotate_to_direction` so the tip aims at the cursor.

- `frames/` — PNG sequence
- `water_arrow_frames.tres` — `SpriteFrames` animation `fly`
- `LICENSE.txt` — Craftpix attribution (M5 visual test exception)
