# Third-party asset packs

Drop full vendor packs here only while evaluating.
Extract used frames into domain folders (e.g. `assets/spells/<id>/`) with a LICENSE.txt.
Do not reference `assets/third_party/` paths from content long-term.
